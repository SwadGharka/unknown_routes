// $(function () {
//     debugger
//   const sections = ['header', 'hero']; // later we’ll add more
//   $.each(sections, function (index, name) {
//     $.get(`components/${name}.html`, function (data) {
//       $('body').append(data);
//       $.getScript(`${name}.js`);
//     });
//   });
// });
